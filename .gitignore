# Python
__pycache__/
*.py[cod]
.pytest_cache/
.venv/
venv/

# OS
.DS_Store

# Skills provisionadas pelo ambiente, não fazem parte deste plugin
.agents/

# Artefatos de runtime de scrapers gerados — nunca commitar cache cru ou segredos
**/data/
*.token
*.secret
.env
.scrape-authorization.json
.scrape-warmup.json
.scrape-session.json

# SDD progress ledger (scratch)
.superpowers/

# Build artifacts (plugin zip)
dist/
