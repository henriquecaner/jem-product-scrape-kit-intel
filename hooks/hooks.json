{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "sh -c 'for p in python3 py python; do command -v \"$p\" >/dev/null 2>&1 && exec \"$p\" \"$CLAUDE_PLUGIN_ROOT/hooks/scripts/precheck.py\"; done; echo \"precheck: no python interpreter found\" >&2; exit 2'"
          }
        ]
      }
    ]
  }
}
